schema_version: 1
id: example-resource
kind: reference
title: "Example resource"
description: "Describe when an agent should use this resource."
path_ref:
  registry: external_resources
  id: example-resource
load_policy: on_demand
index_policy: metadata
license:
  name: unknown
source:
  type: manual
  url: null
update_policy:
  mode: manual
versioning:
  mode: multi_version
  version_field: version
  instance_id_template: "{id}@{version}"
retention:
  policy: keep_versions
  keep_last_n: null
snapshot_behavior:
  on_new_version: notify_only
  on_current_generation_changed: notify_only
  on_instance_missing: broken
  reproducibility: exact
