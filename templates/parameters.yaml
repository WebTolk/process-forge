schema_version: 1
kind: processforge.parameters
scope: project
parameters:
  example:
    active_profile: default
    profiles:
      - id: default
        title: Default local profile
        auth_ref: local.default
