schema_version: 1

product:
  id: processforge
  current_version: "0.1.0"

channels:
  stable:
    latest: "0.1.0"

versions:
  - version: "0.1.0"
    channel: stable
    released_at: "2026-07-15"
    compatibility:
      min_project_schema: 1
      max_project_schema: 1
    migration:
      required: false
      guide: null
    changes:
      - type: baseline
        summary: "Initial linked workplace MVP."
    affected_files: []
