schema_version: 1
id: fixture-resource-import
source: fixture.source.a
topics:
  - fixture.topic.a
mode: plan_only
download:
  enabled: false
  reason: "MVP creates a plan and index records before any mirror/download run."
resources:
  - id: fixture-resource-a
    kind: reference
    title: "Fixture resource mirror"
    path_ref:
      registry: external_resources
      id: fixture-resource-a
    load_policy: on_demand
    index_policy: full_text
    license:
      name: source-specific
    source:
      type: documentation_source
      id: fixture.source.a
      topic: fixture.topic.a
    update_policy:
      mode: manual_plan_first
index_policy: write_resource_index_without_loading_full_content
