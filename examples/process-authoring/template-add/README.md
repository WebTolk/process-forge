# Template Add Process Authoring Example

This example points to the public stable built-in `template-add` as a reference process pack.

- Process: `processes/template-add.yaml`
- Prompt: `prompts/template-add-agent.md`
- Documentation: `docs/processes/template-add.md`

Validate with:

```bash
python bin/pf.py process-doctor --project-root . --process template-add --contract-only
```
