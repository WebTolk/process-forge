# Reusable Template Authoring Process Authoring Example

This example points to the public stable built-in `reusable-template-authoring` as a reference process pack.

- Process: `processes/core/reusable-template-authoring.yaml`
- Prompt: `prompts/reusable-template-authoring-agent.md`
- Documentation: `docs/processes/reusable-template-authoring.md`

Validate with:

```bash
python bin/pf.py process-doctor --project-root . --process reusable-template-authoring --contract-only
```
