# Process Template Install Process Authoring Example

This example points to the public stable built-in `process-template-install` as a reference process pack.

- Process: `processes/process-template-install.yaml`
- Prompt: `prompts/process-template-install-agent.md`
- Documentation: `docs/processes/process-template-install.md`

Validate with:

```bash
python bin/pf.py process-doctor --project-root . --process process-template-install --contract-only
```
