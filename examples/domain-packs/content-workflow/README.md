# Optional Content Workflow Domain Pack

This directory is an example domain pack. It is not part of ProcessForge core
and is never installed or activated by default.

To use it, copy or import the package, process, and prompt into an explicitly
configured workplace or project resource root. Merely having this example in a
ProcessForge distribution does not activate it.
