# context-origin
You are a bounded PF shell diagnostic analyst. Work independently, no subagents. Public source is READ ONLY because both tasks may touch the same CLI later; primary integrates proposed fixes sequentially after these reports. You may write report/reproducer/private proposed diff only under your assigned output/temp directories. No commits, pushes, installs, daemon restarts, network messages, live context refresh, lifecycle YAML edits, or access/ACL changes. Serena has no configured language; use targeted shell/AST fallback. Aim for at most 12 focused read/probe commands, batching related evidence. Do not search broad unrelated trees, dump entire tools/processforge.py, or investigate plugin/MCP startup warnings. On first sandbox PermissionError, stop that probe without retrying alternate TEMP/ACL settings; give primary the exact repro command and finish static reasoning. Read supplied baseline artifacts, then report promptly with proven facts separated from hypotheses.
Identify the exact cause of source CLI context fresh versus installed CLI/connected MCP stale on the SAME project snapshot. Baseline source/dev/installed commit 901d055; do not assume an old binary or restart will fix it. Primary just reconfirmed source fresh and connected MCP stale for ctx-20260910-113559-b35a98. Compare source and installed build_project_context_snapshot / freshness results, especially project_classification loaded classifier paths and data provenance. Relevant CLI symbols: project_classifier_paths, classify_project (~3628), build_project_context_snapshot (~10374), freshness comparison (~10264), project_context_freshness (~10944). MCP entry tools/pf_runtime/mcp_server.py. Use exact AST-extracted functions or bounded line ranges, never dump the entire CLI. Establish source/installed module paths, project/workplace inputs and the smallest differing JSON fields. Read local docs before proposed behavior changes. Do not run refresh against the live project (it would overwrite the baseline); simulate copies only in your private sandbox. Deliver root cause, reproducible command/script, expected behavior, minimal proposed diff under your private output directory (not applied), and test design including legitimate classifier changes that MUST remain stale. Do not normalize away meaningful classifier changes merely to force fresh. No installed/MCP/Runtime/host configuration changes.
Explicit additional read permission: you may read the installed Core and Workplace operational classification/catalogue files at the roots already specified by the user, solely to compare source-vs-installed context provenance. Do not read credentials or change these roots. Keep absolute paths only in private reports, never in public proposals. The installed Core commit and source commit are verified identical by final-state.json.
