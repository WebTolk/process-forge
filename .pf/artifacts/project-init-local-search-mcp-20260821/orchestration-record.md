# Orchestration record

Status: in progress

The delivery is governed by run `project-init-local-search-mcp-20260821` using `software-feature-development`.

- Current context snapshot is fresh, but its health is blocked by unrelated missing capability declarations in the existing `knowledge-package-improvement` route. This run will not silently use a web specialization to mask that gap.
- The first isolated shell-worker task inventories current snapshot resource forms and SQLite FTS5 suitability. It owns only its report.
- Product code remains unchanged until the audit and architecture gates are complete.
