# Correct F02 false blocker for already-owned ancestor removal

Primary found a concrete regression after initial implementation acceptance. Read owned_ancestor_probe.json and owned_ancestor_probe.py in this folder: baseline successfully applies old owned a.txt -> new a.txt/child.txt, while repaired code wrongly blocks with unowned_path_collision. Both original implementation workers have exited and been collected. Primary transfers ownership of src/processforge_core/core_update.py and tools/smoke_core_update_manifest.py to you for this correction. Assurance paused; no other writer on these files. Model Luna/medium, shell PF worker.

Minimal design: lexical existing ADDED target collision must still block regardless of force or identical bytes; unowned ancestor file/symlink must still block. An ancestor that is an old owned regular file, scheduled for removal, may be safely removed before writing the added child; allow that specific transition, while retaining locally_modified blockers and backup/rollback. Pass relevant owned/removed paths into the helper instead of weakening unowned policy globally. Do not allow symlink ancestors, unchanged-owned ancestors, or unrelated user paths. Avoid new abstractions/broad refactors.

Add public smoke regressions: clean owned file-to-directory transition succeeds and writes child; rollback restores original owned file bytes; locally modified owned ancestor blocks without force and preserves bytes; if force permits it under existing contract, successful apply and rollback restore modified bytes; unowned ancestor refusal remains. Keep all other F02 regression cases and public tests. No Git, HEAD loading, private .pf temp path, fixed PID directories or cleanup suppression in public smoke. Use standard TemporaryDirectory.

Rollback boundary clarification: core_update.py exposes backup records and recovery assessment, not a standalone rollback_update API. Do not invent/add rollback implementation or extend CLI. For the restoration criteria above, verify the actual backup record/file contains exact original or forced-local bytes and record that a full rollback execution is outside this API. Preserve existing recovery tests.

CRITICAL environment boundary: Python TemporaryDirectory hits WinError 5 in worker sandbox, same interpreter PASS in primary process. DO NOT diagnose/repair this or probe alternative paths, change permissions, monkeypatch tempfile, modify tests to conceal it, or use D:/tmp. Do not run smokes at all in your sandbox; primary will execute them after your terminal exit. Syntax check once is fine. Report implemented cases and checks NOT RUN honestly; deliver promptly after code and report. Primary has actual regression and full test infrastructure.

Allowed writes: the two public files, .pf/artifacts/core-f01-f02-20260910/f02-owned-ancestor/**. All prior dirty work, .pf state outside your evidence, old audit, checksums, VERSION, tools/processforge.py, installed Core/Workplace are forbidden. Read existing task/capsule and bounded brief; do not rebuild context, invoke other workers, change lifecycle, commit or publish. Serena symbol extraction is unavailable (Active languages: []); use narrow shell reads/search. No web needed. Write concise report.md and log.md in your owned evidence folder, with actual timestamp, changed files, design, coverage and primary verification remaining. Exit after report; no further work.
