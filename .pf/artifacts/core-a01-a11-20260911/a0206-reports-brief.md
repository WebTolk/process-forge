You are a bounded implementation worker, not an auditor. Follow the primary design and fix only your assigned defects. No subagents, no lifecycle commands, no context refresh, no installs/restarts, no commits, no ACL or sandbox changes. Do not modify checksums, VERSION, release registrations or unrelated files. Primary owns integration and all PF transitions.
Use targeted rg/AST reads: Serena has no configured languages. Do not dump tools/processforge.py or scan all .pf. Aim for <= 20 focused tool calls; before code edits use at most 8 reads, each <= 180 lines. Write a failing regression from the supplied defect evidence, make the minimal repair, then run focused tests. On the FIRST sandbox PermissionError stop trying environment workarounds; preserve runnable tests and tell primary the exact command. You can still implement and perform static reasoning. No repeated broad exploration. Finish promptly with a concise report (<= 100 lines), exact changed files, actual test results vs unexecuted checks, risks. Use repo-relative paths in the report and describe absolute-path samples in words. Public tests use disposable temp fixtures, neutral names, no this-machine paths. Write report even on blockers.


Fix A02 first, then A06. Read bounded collector/expected_report_artifact/task_output_path in tools/processforge.py and host.py report authorization/automatic content checks. Old real evidence: recovery-20260911/verify_collection.py and collection-results.json under audit root. Requirements: a single safe project-relative resolver rejects absolute, traversal, drive-relative, Windows/UNC and escaping symlink paths BEFORE file reads in required outputs, worker-run start/collect and host report authorization. Keep legitimate project-relative files and exact expected-output equality. Existing containment helpers may be reused; do not alter unrelated public API. A06: allow absolute-path TEXT in authenticated PF-owned report only after valid task/run/native session/attempt/provenance/path/exact bytes/hash checks; keep secret scanning. Untrusted native assistant text remains filtered. Preserve durable raw ingestion, retry re-derivation and exactly one normalized message. Regression must use actual disposable worker collector+host ingestion, not a mock claiming success. Test traversal/absolute/symlink rejection with no external bytes in raw/chat/result, legitimate report collect twice exactly one, path-containing authenticated report collect twice exactly one, forged IDs/attempt/hash and secrets denied. Never rewrite legacy audit evidence. Primary owns integration registrations.

Governing design: .pf/artifacts/core-a01-a11-20260911/design.md. Audit root: .pf/artifacts/codebase-audit-20260910/.
