You are a bounded implementation worker, not an auditor. Follow the primary design and fix only your assigned defects. No subagents, no lifecycle commands, no context refresh, no installs/restarts, no commits, no ACL or sandbox changes. Do not modify checksums, VERSION, release registrations or unrelated files. Primary owns integration and all PF transitions.
Use targeted rg/AST reads: Serena has no configured languages. Do not dump tools/processforge.py or scan all .pf. Aim for <= 20 focused tool calls; before code edits use at most 8 reads, each <= 180 lines. Write a failing regression from the supplied defect evidence, make the minimal repair, then run focused tests. On the FIRST sandbox PermissionError stop trying environment workarounds; preserve runnable tests and tell primary the exact command. You can still implement and perform static reasoning. No repeated broad exploration. Finish promptly with a concise report (<= 100 lines), exact changed files, actual test results vs unexecuted checks, risks. Use repo-relative paths in the report and describe absolute-path samples in words. Public tests use disposable temp fixtures, neutral names, no this-machine paths. Write report even on blockers.


Fix A08 in native stdio MCP. Current respond/main accepts jsonrpc 1.0 and coerces list params/arguments to {} and responds to notifications. Validate JSON-RPC 2.0 envelope, method/id types (bool is not numeric ID), params and tools/call arguments using existing advertised tool schema constraints before dispatch. Preserve missing optional args/defaults and valid clients. Errors: parse -32700; invalid envelope -32600; invalid params/schema -32602; unknown method -32601. Valid notifications never emit responses including tools/call errors, but valid mutating notifications retain the chosen spec-compliant dispatch behavior. Distinguish id absent from explicit null. Use local JSONRPC/MCP docs or existing tests, no new third-party dependency. Regression sends real newline-framed requests to source native stdio MCP with disposable workplace, tests malformed JSON recovery, wrong protocol/method/id, params and arguments arrays/scalars, required/type/unknown arguments, valid initialize/list/tool call, notification silence using subsequent sentinel response. A fake runtime can supplement dispatch count but is not sole protocol proof. Do not change business/session logic.

Governing design: .pf/artifacts/core-a01-a11-20260911/design.md. Audit root: .pf/artifacts/codebase-audit-20260910/.
