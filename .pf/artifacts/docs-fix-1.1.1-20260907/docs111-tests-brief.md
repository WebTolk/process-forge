# Regression tests for documented CLI and contracts

## Corrective attempt 2 — read first, narrow scope

The previous gpt-5.4-mini attempt was STOPPED by the orchestrator for introducing a private assignment dependency in a PUBLIC smoke. You are gpt-5.6-luna, replacing that attempt. Rewrite tools/smoke_docs_current_code_contract.py to a sound, portable test. It MUST NOT read or mention docs111 task ids, private assignments, this audit folder, or private reports. Load YAML front matter from the fenced EXAMPLE in docs/concepts/assignment-front-matter.md and pass its required_outputs through processforge.normalize_required_outputs; assert nonempty and explicit id/path/type. Verify the actual example, not this worker's assignment.

Specific defects to correct in the draft: CLI scanner misses root Markdown and processforge.py launchers, excludes PowerShell fences, doesn't combine continuations or normalize placeholders, and uses posix=False quote handling. Use the private inventory algorithm as a read-only design reference ONLY if needed, but write standalone public code; no private imports or file references in public code. Check existing local Markdown target links too (ignore URLs and anchors, clearly state limits). Require substantial coverage, not merely count>0. Use a robust import of tools/processforge.py and parser.parse_args only, never dispatch commands. Capture argparse stderr for readable failures. Test every changed update plan/apply line has --workplace-root, not merely one marker somewhere. Parse the actual JSON transition payloads in EN/RU declarative concepts, validate artifact records and gate status using the current contract; distinguish state excerpts from payloads. Add tests that reject id-keyed required_outputs and gate status pass. Avoid exact English sentences as assertions; preserve intent through stable API markers and data checks.

Keep the correct search-index removal in tools/smoke_context_freshness_vs_execution_readiness.py, but undo unrelated system-temp -> repository-temp relocation: isolated system TemporaryDirectory was already valid and safer for extracted read-only installs. Tighten the specialized prompt check in smoke_docs_agent_no_manual_infra.py using stable workflow markers and the absence of executable manual lifecycle commands; avoid brittle complete sentence matches. Main already registered the new smoke in tools/processforge.py; DO NOT edit that file or checksum inventory. Run the three assigned tests and public-cleanliness. Work ONLY on those three test files. No lifecycle commands, no giant log scans, no unrelated source reads. Report precise executed results, not grep labelled as parser validation.

You are a bounded PF shell worker explicitly requested by the operator. The orchestrator owns Run/Task lifecycle and .pf state: do NOT call pf.work.start/transition, session-start, task-complete, update manifest/capsules, start infrastructure, or delegate. Read your assignment capsule and this brief; no full-project bootstrap is needed. Use apply_patch for edits; read UTF-8 explicitly on PowerShell. Preserve all existing unrelated changes, especially tools/processforge.py and the required-output fix. Write only assigned files; do not change VERSION, CHANGELOG, checksums or publish/install/update anything. No network research is needed: current local source is authority. Repository temp dirs only .pf/tmp/. Return a complete Markdown report as your final response, with timestamp, files changed, exact checks/results and residual risks; the launcher captures it as expected report. Do not claim completion on mere file existence.

## Assignment

After all three documentation workers complete, implement regression protection for D01-D08. New standalone smoke tools/smoke_docs_current_code_contract.py: check published fenced PF CLI examples against current build_parser without executing side effects (skip prose intentionally; tolerate placeholders/continuations); parse actual assignment front matter and prove normalized required_outputs contain path; validate current task-batch ordinary workflow, full evidence shape/status and multi-process choice documented EN/RU; ensure Russian update examples request workplace root, and checksum behavior text does not claim capsule validation. Avoid brittle exact full sentence checks, test behavior where possible, keep fast/network-free and portable. You may reuse ideas from .pf/artifacts/docs-audit-1.1.1-evidence/audit_inventory.py but do not import .pf in public tests or reference private audit paths publicly. Expand existing no-manual-infra smoke for specialized task-batch prompt without false banning qualified operator examples. Fix old smoke_context_freshness_vs_execution_readiness.py's search-index refresh unsupported --project-root to current Workplace-only contract and adjust only related assumptions if needed. No weakening assertions merely to pass. Main orchestrator owns release smoke registration in tools/processforge.py; tell it needed registration. Run your three tests and report exact results. Do not touch production logic or checksums.

## Allowed product writes

## Final implementation handoff

The orchestrator corrected documentation-worker mistakes; use current files as input. All CLI examples and 299 local file links now pass private inventory. Include local link target checking in the public regression to catch the broken RU Garage link that the worker introduced. Focus reads on your three target test files, relevant doc sections, and exact code definitions. Do not read giant runtime logs, full .pf snapshots, all tools files, or whole unrelated docs. Do not inspect/prepare/stop your own worker-run or mutate lifecycle metadata. The final report is captured by the launcher. Existing required-output normalization is a LIST of records with id/path, not a mapping keyed by id. Gate evidence is attestation, not independent semantic proof. Do not register the smoke in tools/processforge.py; main owns it. Limit this task to implementation and executing your three tests, not a full release-test.

- tools/smoke_docs_current_code_contract.py
- tools/smoke_docs_agent_no_manual_infra.py
- tools/smoke_context_freshness_vs_execution_readiness.py
