# Task Index: Work State And Resource Constraints

1. Separate incomplete stage obligations from actual blocked work in
   `pf.work.state`, with a regression covering both states.
2. Make explicit knowledge-resource `preferred_version` and `constraint`
   selectors fail closed, retaining an unresolved provenance record.
3. Refresh the live Joomla plugin snapshot, verify selected resources and
   full-text documentation search, then create a governed agent assignment
   against that snapshot.
