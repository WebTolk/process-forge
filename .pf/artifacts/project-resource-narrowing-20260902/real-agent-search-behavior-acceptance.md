# Real Agent Search Behavior Acceptance

Status: pass

The real-project-shaped query returned a current documentation result with
`match_reason: content`. The selected-result resolver denied an item found
only in `available_knowledge_resources`, proving that diagnostic availability
cannot become an implicit authority grant.

No workplace-wide, web, or historical-version fallback was used.
