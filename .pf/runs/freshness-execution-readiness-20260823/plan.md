# Run Plan: Freshness vs execution readiness release blocker

Objective: Separate context/resource freshness from execution readiness so read-only MCP search/resolve can use fresh authorized resources even when current execution capabilities are missing.
