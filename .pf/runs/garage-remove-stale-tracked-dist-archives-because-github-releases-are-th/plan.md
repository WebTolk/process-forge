# Run Plan: Remove stale tracked dist archives because GitHub releases are the public distri

Objective: Remove stale tracked dist archives because GitHub releases are the public distribution channel, and enforce sequential Core then Workplace then project-scoped migration policy; add focused regression coverage
