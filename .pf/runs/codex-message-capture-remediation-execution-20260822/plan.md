# Run Plan: Codex message capture remediation execution

Objective: Implement and independently verify durable Codex user-message capture ordering, conditional SessionEnd assistant fallback, and Ledger-bound MCP transcript evidence using a writable but workspace-scoped shell-worker profile.
