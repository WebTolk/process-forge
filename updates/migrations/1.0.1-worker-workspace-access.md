# ProcessForge 1.0.1 Migration

No migration is required.

Projects can keep existing assignments and runtime drivers. New shell-agent
flows may declare `workspace_access` in assignments or orchestrator plans to
grant workplace knowledge, template, tool, and MCP references through a private
runtime access file.

Private absolute paths must stay out of assignment and capsule artifacts. Use
`path_ref` or registry/resource ids instead.
