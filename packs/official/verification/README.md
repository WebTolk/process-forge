# Official Verification Process Pack

This directory is the canonical source of the production-grade verification
pack bundled with ProcessForge. It is distribution data, not part of the
domain-neutral runtime kernel.

The pack is available by default but inactive until selected by the
`verification` profile or explicit pack activation. Its process and declared
capabilities participate in resolution only while the pack is active.
