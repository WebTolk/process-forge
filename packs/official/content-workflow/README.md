# Official Content Workflow Process Pack

This directory is the canonical source of the production-grade content
workflow pack bundled with ProcessForge. It is distribution data, not part of
the domain-neutral runtime kernel.

The pack is available by default but inactive until selected by the
`content-workflow` profile or explicit pack activation. Its processes and
declared capabilities participate in resolution only while the pack is active.
